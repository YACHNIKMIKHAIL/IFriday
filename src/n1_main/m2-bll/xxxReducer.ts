let initState: any = []

export const XxxReducer = (state = initState, action: any) => {
    switch (action.type) {
        case 'xxx': {
            return state
        }
        default:
            return state
    }
}